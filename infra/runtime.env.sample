PROJECT_ID=demo-gcp-project
REGION=us-central1
INTAKE_BUCKET=demo-intake-bucket
OUTPUT_BUCKET=demo-output-bucket
SUMMARY_BUCKET=${OUTPUT_BUCKET}
DOC_AI_SPLITTER_PROCESSOR_ID=projects/${PROJECT_ID}/locations/${REGION}/processors/splitter-id
DOC_AI_PROCESSOR_ID=projects/${PROJECT_ID}/locations/${REGION}/processors/processor-id
OPENAI_API_KEY=stub-openai-key
WORKFLOW_NAME=projects/${PROJECT_ID}/locations/${REGION}/workflows/demo-ocr-pipeline
SUMMARISER_JOB_NAME=demo-ocr-summariser
PDF_JOB_NAME=demo-ocr-pdf-writer
PIPELINE_SERVICE_BASE_URL=https://demo-ocr-summary-${REGION}-a.run.app
PIPELINE_STATE_BACKEND=gcs
PIPELINE_STATE_BUCKET=demo-ocr-state
PIPELINE_STATE_PREFIX=pipeline-state
PIPELINE_DLQ_TOPIC=demo-ocr-pipeline-dlq
SUMMARY_SCHEMA_VERSION=2025-10-01
MAX_SHARD_CONCURRENCY=12
INTERNAL_EVENT_TOKEN=stub-internal-token

# NOTE:
# Secrets are sourced at runtime via Secret Manager (Cloud Run --set-secrets).
# Leave secret values empty in this file and manage bindings through deployment tooling.
